react practice with a quote API
first try with semantic UI instead of SCSS. 

random quote generated via a button
router created for all pages (including loader and 404 not found)
searchbar implementation
